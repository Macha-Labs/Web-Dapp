import { FlexWindow } from "@/_ui/flex/FlexWindow";
import AuthCard from "@/components/auth/AuthCard";

function Auth() {
  return (
    <FlexWindow>
      <AuthCard />
    </FlexWindow>
  );
}

export default Auth;
